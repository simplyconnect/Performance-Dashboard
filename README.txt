Put your logo files here:
  logo.png       - full wordmark, used as the browser favicon
  logo-mark.png  - just the icon/swirl, used in the sidebar (32x32 or larger, square)

The dashboard works fine without them (the sidebar just shows the text
"Simply Connect" and the browser falls back to its default favicon) —
these were not included in the files you uploaded, so add them back
from your original project before deploying.
