/**
 * js/config.js
 * For a shared deploy (e.g. on Vercel) where everyone should connect
 * automatically with no per-person setup step, paste your Apps Script
 * Web App URL below and commit this file.
 *
 * Leave API_URL blank to let each person paste their own URL in the
 * dashboard's "API Setup" tab instead (saved to that browser only).
 */
window.DASH_CONFIG = {
  API_URL: '',
  DEFAULT_DAYS: 90
};
